<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Status Update</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; margin: 20px; background-color: #f4f4f4; }
        .container { max-width: 600px; margin: auto; padding: 20px; background-color: #fff; border: 1px solid #ddd; border-radius: 5px; }
        .header { font-size: 24px; font-weight: bold; color: #17a2b8; margin-bottom: 20px; }
        .content p { margin: 15px 0; }
        .status-badge {
            display: inline-block;
            padding: 5px 10px;
            font-size: 1em;
            font-weight: bold;
            color: #fff;
            border-radius: 4px;
            text-transform: capitalize;
        }
        .status-shipped { background-color: #007bff; }
        .status-delivered { background-color: #28a745; }
        .status-confirmed { background-color: #ffc107; color: #333;}
        .status-pending { background-color: #6c757d; }
        .status-cancelled { background-color: #dc3545; }
        .button {
            display: inline-block;
            padding: 10px 20px;
            margin-top: 15px;
            font-size: 15px;
            color: #fff;
            background-color: #007bff;
            text-decoration: none;
            border-radius: 5px;
        }
        .footer { font-size: 0.9em; color: #777; margin-top: 30px; text-align: center; border-top: 1px solid #eee; padding-top: 15px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            Order #<%= typeof orderId !== 'undefined' && orderId ? orderId : '[Unknown Order]' %> Status Update
        </div>
        <div class="content">
            <p>Hello<%= typeof userName !== 'undefined' && userName ? ' ' + userName : '' %>,</p>
            
            <% if (typeof newStatus !== 'undefined' && newStatus) { %>
                <p>The status of your order #<strong><%= typeof orderId !== 'undefined' && orderId ? orderId : '[Unknown Order]' %></strong> has been updated to: 
                    <span class="status-badge status-<%= newStatus.toLowerCase() %>"><%= newStatus %></span>
                </p>
                <% if (newStatus.toLowerCase() === 'shipped' && typeof trackingLink !== 'undefined' && trackingLink) { %>
                    <p>You can track your shipment here: <a href="<%= trackingLink %>"><%= trackingLink %></a></p>
                <% } %>
            <% } else { %>
                <p>There has been an update regarding your order #<strong><%= typeof orderId !== 'undefined' && orderId ? orderId : '[Unknown Order]' %></strong>.</p>
            <% } %>

            <% if (typeof orderDetailsLink !== 'undefined' && orderDetailsLink) { %>
                <p>For more details, you can view your order here:</p>
                <a href="<%= orderDetailsLink %>" class="button">View Order Details</a>
            <% } %>
        </div>
        <div class="footer">
            <p>&copy; <%= new Date().getFullYear() %> Your Company. All rights reserved.</p>
        </div>
    </div>
</body>
</html> 