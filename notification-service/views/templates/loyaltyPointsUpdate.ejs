<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Loyalty Points Update</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; margin: 20px; background-color: #f4f4f4; }
        .container { max-width: 600px; margin: auto; padding: 20px; background-color: #fff; border: 1px solid #ddd; border-radius: 5px; }
        .header { font-size: 24px; font-weight: bold; color: #ffc107; margin-bottom: 20px; }
        .content p { margin: 10px 0; }
        .points-earned { color: #28a745; font-weight: bold; }
        .points-spent { color: #dc3545; font-weight: bold; }
        .total-points { font-size: 1.2em; font-weight: bold; margin-top: 15px; }
        .footer { font-size: 0.9em; color: #777; margin-top: 30px; text-align: center; border-top: 1px solid #eee; padding-top: 15px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            Loyalty Points Update
        </div>
        <div class="content">
            <p>Hello<%= typeof userName !== 'undefined' && userName ? ' ' + userName : '' %>,</p>

            <p>There has been an update to your loyalty points balance.</p>

            <% if (typeof transactionDescription !== 'undefined' && transactionDescription) { %>
                <p><strong>Details:</strong> <%= transactionDescription %></p>
            <% } %>

            <% if (typeof pointsEarned !== 'undefined' && pointsEarned > 0) { %>
                <p>You have <span class="points-earned">earned <%= pointsEarned %> points</span>.</p>
            <% } %>

            <% if (typeof pointsSpent !== 'undefined' && pointsSpent > 0) { %>
                <p>You have <span class="points-spent">spent <%= pointsSpent %> points</span>.</p>
            <% } %>

            <% if (typeof newTotalPoints !== 'undefined') { %>
                <p class="total-points">Your new loyalty points balance is: <%= newTotalPoints %> points.</p>
            <% } else { %>
                <p>Please check your account for your latest loyalty points balance.</p>
            <% } %>
        </div>
        <div class="footer">
            <p>&copy; <%= new Date().getFullYear() %> Your Company. All rights reserved.</p>
        </div>
    </div>
</body>
</html> 